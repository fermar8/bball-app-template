# Database dependencies
# Using psycopg (version 3) for better Windows compatibility
psycopg[binary,pool]>=3.1.0,<4.0.0

# AWS SDK (if needed for secrets manager, etc.)
boto3==1.34.162

# Development dependencies
pytest==8.0.0
pytest-cov==4.1.0
pytest-mock==3.12.0
poethepoet==0.39.0
